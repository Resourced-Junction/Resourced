# Resourced

## Stream structure

Messages will have the following structure with a variable count of data elements

```json
{
  "data": [
    {
      "address": "string",
      "key": "string"
    },
    {
      "address": "string",
      "key": "string"
    }
  ]
}
```
